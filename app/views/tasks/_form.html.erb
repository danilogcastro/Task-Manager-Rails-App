<%= simple_form_for(@task) do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <% unless @task.new_record? %>
    <%= f.input :completed %>
  <% end %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>

<hr>

<%= link_to "Back to list", tasks_path, class: "btn btn-info"  %>
